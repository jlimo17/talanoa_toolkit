import React from "react";
import ReactDOM from "react-dom/client";
import TalanoaToolkit from "./TalanoaToolkit.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TalanoaToolkit />
  </React.StrictMode>
);
